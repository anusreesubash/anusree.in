import React from 'react';
import { Section, SectionTitle, SectionSubtitle } from '../../styles/GlobalComponents';

const Resume = () => (
  <Section nopadding id="projects">
    <SectionTitle main>Resume</SectionTitle>
    <SectionSubtitle main>Employment</SectionSubtitle>
    <SectionSubtitle main>Skills</SectionSubtitle>
    <SectionSubtitle main>Education</SectionSubtitle>
  </Section>
);

export default Resume;